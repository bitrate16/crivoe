1. Using AL, ML, NN or any machine learning algorhitms (inclusing, but not limited to Copilot-like software, transformets models, GPT models, BERT models, textual predictors and / or vectorization models, AI (ML) -based codegeneration, e.t.c) on / with this source and / or compiled binaries (including, but not limited to passing them as input, using as train data, using content scanners, transforming, indexing, using in validation, e.t.c) is strictly prohibited. This restriction includes using this code as training data for any kind of generative / suggestion / transformation models and / or algorhitms.
2. Take care of your cat(s);
